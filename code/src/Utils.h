#pragma once

#include <glm\glm.hpp>
#include <glm/gtc/type_ptr.hpp>
#include <glm/vec3.hpp>


//void BoxBounce(glm::vec3 pos)
//{
//	pos = pos - (1 + 1.0f) * (glm::dot(glm::normalize(glm::cross(glm::vec3(-5, 0, 5), glm::vec3(5, 0, 5))), pos + (pos.y - 0.1f))) * glm::normalize(glm::cross(glm::vec3(-5, 0, 5), glm::vec3(5, 0, 5)));
//}